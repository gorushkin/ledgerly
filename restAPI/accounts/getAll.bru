meta {
  name: getAll
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}{{accounts}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:post-response {
  accountId1: res.body[0].id
  accountId2: res.body[1].id
}
