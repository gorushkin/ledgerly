export type Currency = {
  code: string; // ISO 4217 currency code, e.g., "USD", "EUR"
  name: string; // Full name of the currency, e.g., "United States Dollar", "Euro"
  symbol: string; // Symbol used for the currency, e.g., "$", "€"
};
