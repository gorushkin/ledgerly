/**
 * The expected number of user (non-system) operations per entry in transaction responses.
 * Each entry should contain exactly 2 user operations representing the double-entry bookkeeping pattern.
 */
export const REQUIRED_USER_OPERATIONS_PER_ENTRY = 2;
