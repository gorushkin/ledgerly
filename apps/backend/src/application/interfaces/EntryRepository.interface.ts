// import { UUID } from '@ledgerly/shared/types';

// import { Entry } from '../../domain/entries:toRefactor/entry.entity';

// export type EntryRepository = {
//   save(entry: Entry): Promise<Entry>;
//   getById(userId: UUID, id: UUID): Promise<Entry | null>;
//   getAllByTransactionId(userId: UUID, transactionId: UUID): Promise<Entry[]>;
//   delete(userId: UUID, id: UUID): Promise<boolean>;
//   exists(userId: UUID, id: UUID): Promise<boolean>;
// };
